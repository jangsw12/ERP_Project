<UserControl x:Class="ERP_Project.Views.BOMView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ERP_Project.Views"
             xmlns:cont="clr-namespace:ERP_Project.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="root">
    <UserControl.Resources>
        <cont:PageNumberConverter x:Key="PageNumberConverter"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--Search-->
        <Border Grid.Row="0"
                Background="#ECF0F1"
                Padding="10"
                CornerRadius="4">
            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Center">
                <TextBlock Text="상위품목코드"
                           VerticalAlignment="Center"
                           Margin="0 0 5 0"/>
                <ComboBox Width="150"
                         Margin="0 0 15 0"
                         ItemsSource="{Binding ParentItemsLookup}"
                         SelectedValuePath="ItemId"
                         DisplayMemberPath="ItemName"
                         SelectedValue="{Binding SearchParentItemId}"/>

                <TextBlock Text="하위품목코드"
                           VerticalAlignment="Center"
                           Margin="0 0 5 0"/>
                <ComboBox Width="150"
                          Margin="0 0 15 0"
                          ItemsSource="{Binding ChildItemsLookup}"
                          SelectedValuePath="ItemId"
                          DisplayMemberPath="ItemName"
                          SelectedValue="{Binding SearchChildItemId}"/>

                <Button Content="조회"
                        Width="80"
                        Command="{Binding SearchCommand}"/>
            </StackPanel>
        </Border>

        <!--CRUD Buttons-->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0 10 0 10">
            <Button Content="신규"
                    Width="80"
                    Margin="5 0"
                    Command="{Binding AddCommand}"/>
            <Button Content="저장"
                    Width="80"
                    Margin="5 0"
                    Command="{Binding SaveCommand}"/>
            <Button Content="삭제"
                    Width="80"
                    Margin="5 0"
                    Command="{Binding DeleteCommand}"/>
        </StackPanel>

        <!--DataGrid-->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding BOMs}"
                  SelectedItem="{Binding SelectedBOM}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="False"
                  SelectionMode="Single"
                  RowHeaderWidth="20">
            <DataGrid.Columns>
                <DataGridTextColumn Header="(상위)품목코드"
                                    Binding="{Binding ParentItemCode, UpdateSourceTrigger=PropertyChanged}"
                                    Width="120"/>
                <DataGridTextColumn Header="(상위)품목명"
                                    Binding="{Binding ParentItemName, UpdateSourceTrigger=PropertyChanged}"
                                    Width="150"/>
                <DataGridTextColumn Header="(하위)품목코드"
                                    Binding="{Binding ChildItemCode, UpdateSourceTrigger=PropertyChanged}"
                                    Width="120"/>
                <DataGridTextColumn Header="(하위)품목명"
                                    Binding="{Binding ChildItemName, UpdateSourceTrigger=PropertyChanged}"
                                    Width="150"/>
                <DataGridTextColumn Header="수량"
                                    Binding="{Binding Qty, UpdateSourceTrigger=PropertyChanged}"
                                    Width="100"/>
                <DataGridTextColumn Header="로스율"
                                    Binding="{Binding LossRate, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}"
                                    Width="120"/>
                <DataGridCheckBoxColumn Header="사용"
                                        Binding="{Binding IsActive, UpdateSourceTrigger=PropertyChanged}"
                                        Width="80"/>
                <DataGridTextColumn Header="생성일"
                                    Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd}"
                                    IsReadOnly="True"
                                    Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!--Paging Area-->
        <Grid Grid.Row="3"
              Margin="0 10 0 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!--TotalCount-->
            <TextBlock Grid.Column="0"
                       VerticalAlignment="Center"
                       Text="{Binding TotalCount, StringFormat=총 {0}건}"
                       FontWeight="SemiBold"/>

            <!--Move Page-->
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center">
                <Button Content="◀ 이전"
                        Width="80"
                        Margin="5 0"
                        Command="{Binding PrevPageCommand}"/>

                <TextBlock Margin="10 0"
                           VerticalAlignment="Center"
                           FontWeight="Bold">
                    <TextBlock.Text>
                        <MultiBinding Converter="{StaticResource PageNumberConverter}" Mode="OneWay">
                            <Binding Path="PageNumber"/>
                            <Binding Path="TotalPages"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>

                <Button Content="다음 ▶"
                        Width="80"
                        Margin="5 0"
                        Command="{Binding NextPageCommand}"/>
            </StackPanel>

            <!--Choose PageSize-->
            <StackPanel Grid.Column="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <TextBlock Text="페이지크기"
                           VerticalAlignment="Center"
                           Margin="0 0 5 0"/>
                <ComboBox Width="70"
                          ItemsSource="{Binding PageSizeOptions}"
                          SelectedItem="{Binding PageSize}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
